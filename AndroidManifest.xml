<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hang.christina.flashcards"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="hang.christina.flashcards.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="hang.christina.flashcards.ManageDeck"
            android:label="@string/title_activity_manage_deck"
            android:parentActivityName="hang.christina.flashcards.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="hang.christina.flashcards.MainActivity" />
        </activity>
        <activity
            android:name="hang.christina.flashcards.DeckList"
            android:label="@string/title_activity_deck_list"
            android:parentActivityName="hang.christina.flashcards.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="hang.christina.flashcards.MainActivity" />
        </activity>
    </application>

</manifest>
